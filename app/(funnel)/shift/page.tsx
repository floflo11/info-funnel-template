import { StepValue } from "@/components/step-value";

export default function ShiftPage() {
  return (
    <StepValue
      heading="Your new routine"
      body="Wake up. Open the guide. Know exactly what to work on today. No guessing, no overwhelm."
      currentPath="/shift"
    />
  );
}
