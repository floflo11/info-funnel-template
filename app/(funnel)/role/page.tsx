import { StepValue } from "@/components/step-value";

export default function RolePage() {
  return (
    <StepValue
      heading="Your only job"
      body="Read one chapter a day. Apply the worksheet. That's it — we handle the strategy."
      currentPath="/role"
    />
  );
}
