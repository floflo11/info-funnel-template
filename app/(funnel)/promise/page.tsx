import { StepValue } from "@/components/step-value";

export default function PromisePage() {
  return (
    <StepValue
      heading="What you won't need to do"
      body="No figuring out algorithms. No posting daily without a plan. No wasting months on strategies that don't convert."
      currentPath="/promise"
    />
  );
}
