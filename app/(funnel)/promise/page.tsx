import { StepSocialProof } from "@/components/step-social-proof";

export default function PromisePage() {
  return (
    <StepSocialProof
      stat="10x"
      caption="Creators who follow a *proven system* earn 10x more than those who wing it"
      subcaption="Based on self-reported data from customers who completed the program."
      currentPath="/promise"
    />
  );
}
